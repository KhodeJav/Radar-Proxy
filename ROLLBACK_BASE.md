# Rollback base

This working tree was restored from `ProxyLink-redesigned-source.tar.gz`, the earlier complete source snapshot selected after auditing the recent regression. All modifications in this iteration must be additive; do not remove existing screens, parser paths, ping actions, settings, background work, or tests.
